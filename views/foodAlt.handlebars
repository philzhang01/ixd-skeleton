<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>Food</title>
  <link rel="stylesheet" type="text/css" href="css/food.css" />
  <link rel="stylesheet"
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
    crossorigin="anonymous" />
  <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
</head>

<body>
  <div class="container food">
    <h3 class="information" id="ppg">Food For This Week</h3>

    {{#if target}}
    {{#if plan}}
    <span class="goal_cal">Your daily total caloric intake for 3 meals is
      {{target}} kcal. Your current goal is to {{plan}} weight. You may
      customize the plan below.
    </span>
    {{/if}}
    {{/if}}


    {{#each food}}
    <h4>{{day}}</h4>
    <form role="form" class="altForm">
      <div class="form-group row">
        <label for="inputAge" class="col-sm-3 col-form-label">Breakfast</label>
        <div class="col-sm-6">
          <input type="text" class="form-control" value="{{breakfast}}"
            name="breakfast{{day}}" />
        </div>
      </div>
      <div class="form-group row">
        <label for="inputGender" class="col-sm-3 col-form-label">Lunch</label>
        <div class="col-sm-6">
          <input type="text" class="form-control" value="{{lunch}}"
            name="lunch{{day}}" />
        </div>
      </div>
      <div class="form-group row">
        <label for="inputHeight" class="col-sm-3 col-form-label">Dinner</label>
        <div class="col-sm-6">
          <input type="text" class="form-control" value="{{dinner}}"
            name="dinner{{day}}" />
        </div>
      </div>
      <button type="submit" class="submitBtnAlt btn btn-warning"
        day="{{day}}">Update</button>
    </form>
    <span class="form-border"></span>
    {{/each}}

    <a href="home" type="button" class="btn btn-warning">Back</a>
  </div>
</body>
<script src="https://code.jquery.com/jquery-3.3.1.min.js"
  crossorigin="anonymous"></script>
<script src="/js/calculateBMR.js"></script>

</html>